# mid-ecs

Data-oriented Entity Component System using a hybrid Archetype-core /
Sparse-set-shell layout, built on `mid-collections`. Benchmarked directly
against `bevy_ecs` (see [Benchmarks](/benchmarks/)).

**Status:** started — archetype core and sparse shell both underway.
