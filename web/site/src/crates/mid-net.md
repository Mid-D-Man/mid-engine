# mid-net

Reliable UDP transport with a hand-rolled wire codec, targeting a 128 Hz
tick rate, plus a DixScript-based transport layer (native via `quinn`, and a
`wasm32` browser variant). Awaiting ECS integration.

**Status:** in progress.
