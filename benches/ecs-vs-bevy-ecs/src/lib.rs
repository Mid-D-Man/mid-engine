//! No library code — this crate exists solely to host
//! `benches/vs_bevy_ecs.rs`, comparing mid-ecs's storage engine against
//! bevy_ecs's on equivalent workloads. See that file and this crate's
//! `Cargo.toml` for why it's a separate workspace member.
