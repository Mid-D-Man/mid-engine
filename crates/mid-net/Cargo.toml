[package]
name = "mid-net"
version = "0.1.0"
edition = "2021"

[lib]
name = "mid_net"
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
mid-net-wire      = { path = "wire" }
mid-net-transport = { path = "transport" }
mid-net-reliable  = { path = "reliable" }
