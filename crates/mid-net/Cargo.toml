[package]
name = "mid-net"
version = "0.1.0"
edition = "2021"

[lib]
name = "mid_net"
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
dixscript = "1.0.0"
tokio     = { version = "1", features = ["full"] }
bytes     = "1.5"
bincode   = "1.3"

[features]
default = []
