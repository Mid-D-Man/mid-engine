[package]
name = "mid-ecs"
version = "0.1.0"
edition = "2021"

[lib]
name = "mid_ecs"
crate-type = ["lib", "cdylib", "staticlib"]

[dependencies]
mid-collections = { path = "../mid-collections" }
rayon = "1.8"

[features]
default = []
